# AES catalog. Package names verified against homebrew formulae and the Debian package index.
name: opencode
description: opencode terminal agent
category: ai
tested: false
tags:
  - ai
dependencies:
  - node
verify:
  command: opencode
  version:
    command: opencode --version
    min: "1.0"
install:
  darwin:
    strategy: npm
    npm_package: opencode-ai@1.18.32
  linux:
    strategy: npm
    npm_package: opencode-ai@1.18.32
